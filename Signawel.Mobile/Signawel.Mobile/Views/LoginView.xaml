<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Signawel.Mobile.Views.LoginView"
             Visual="Material"
             Title="Aanmelden">
    <ContentPage.Content>
        <StackLayout>
            <Entry Placeholder="Email" Text="{Binding Email}" Margin="10" />
            <Entry Placeholder="Wachtwoord" IsPassword="True" Text="{Binding Password}" Margin="10" />
            <Button Command="{Binding ButtonTappedCommand}" Text="Aanmelden" Margin="10" BackgroundColor="Accent"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>